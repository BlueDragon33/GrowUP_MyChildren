{
  "name": "growup-mychildren",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Long-term child learning, health and development PWA for ages 3-18.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/core/model.js && node --check src/core/store.js && node --check src/core/insights.js",
    "verify": "npm run check && npm test"
  }
}
